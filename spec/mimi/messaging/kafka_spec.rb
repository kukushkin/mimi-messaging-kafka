RSpec.describe Mimi::Messaging::Kafka do
  it "has a version number" do
    expect(Mimi::Messaging::Kafka::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
